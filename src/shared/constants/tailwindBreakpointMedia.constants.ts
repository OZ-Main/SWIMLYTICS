/**
 * Media queries aligned with Tailwind default breakpoints (`tailwind.config.ts`).
 * Use with `useMediaQuery` or `window.matchMedia` so JS layout logic matches CSS.
 */
export const MEDIA_QUERY_MIN_SM = '(min-width: 640px)' as const

export const MEDIA_QUERY_MIN_MD = '(min-width: 768px)' as const
