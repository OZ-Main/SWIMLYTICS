export const PERSONAL_BEST_TIME = {
  MAX_MINUTES: 120,
  MAX_SECONDS_FRACTION: 59.99,
} as const

export const PERSONAL_BEST_NOTES = {
  MAX_LENGTH: 2000,
} as const

export const PERSONAL_BEST_FORM_DEFAULTS = {
  TIME_MINUTES: 1,
  TIME_SECONDS: 30,
} as const
