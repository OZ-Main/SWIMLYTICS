import { cva } from 'class-variance-authority'

export const authOAuthDividerRowVariants = cva('flex items-center gap-tight py-stack')

export const authOAuthDividerLabelVariants = cva('shrink-0 text-caption text-muted-foreground')
